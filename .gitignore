### Файлы сборки и временные файлы ###
*.apk           # Готовые APK-файлы (их можно собрать заново)
*.aar           # Android Archive (AAR) — билды библиотек
*.ap_           # Временные файлы упаковки APK
*.aab           # Android App Bundle (AAB) — формат для Google Play

### Папки, создаваемые Gradle и Android Studio ###
.gradle/        # Кеш Gradle (пересоздаётся при сборке)
build/          # Папка с промежуточными билдами (генерируется автоматически)
out/            # Выходные файлы (старая версия папки build)
bin/            # Устаревшая папка для скомпилированных файлов
gen/            # Устаревшая папка для сгенерированного кода (R.java и т.д.)

### Настройки IDE (Android Studio) ###
.idea/          # Конфиги и кеш Android Studio
*.iml           # Файлы конфигурации модулей
local.properties # Локальные настройки SDK (содержит пути, которые могут быть разными на разных ПК)

### Файлы операционной системы ###
.DS_Store       # Служебный файл macOS (если работаете на Mac)
Thumbs.db       # Кеш иконок Windows

### Логи и временные файлы ###
captures/       # Скриншоты и видео из эмулятора
*.log           # Логи сборки
*.tmp           # Временные файлы

### Другие специфичные файлы ###
/release/       # Папка для релизных APK (если создаёте вручную)
/proguard/      # Файлы оптимизации ProGuard (если не нужны в репозитории)